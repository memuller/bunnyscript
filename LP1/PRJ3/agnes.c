#include <stdio.c>

int main()
{
    char resp;
    printf("OI AGNES\nTUDO BEM COM VOCÊ? (y/n)");
    scanf("%c", resp);
    if (resp == "y")
    {
        printf ("po bacana incrível sensacional muito bom eceleite\n");
    } else if (resp == "n")
    {
        printf ("poxa, que bad\n");
    } else {
        printf("para de ser mongol e digita a porra de uma letra porra");
    }
    }
}